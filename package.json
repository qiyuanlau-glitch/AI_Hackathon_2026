{
  "scripts": {
    "build": "node build.mjs",
    "deploy": "./deploy-worker.sh",
    "deploy:pages": "./deploy.sh"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "wrangler": "^4.99.0"
  }
}
